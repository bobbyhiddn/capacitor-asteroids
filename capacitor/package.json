{
  "name": "capacitor-app",
  "version": "1.0.0",
  "description": "An Amazing Capacitor App",
  "type": "module",
  "keywords": [
    "capacitor",
    "mobile"
  ],
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "ios:prepare": "npm run build && npx cap sync ios && node scripts/copy-assets.js",
    "ios:build": "npm run ios:prepare && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "latest",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/splash-screen": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest",
    "fs-extra": "^11.2.0",
    "path": "^0.12.7",
    "vite": "^5.0.0"
  },
  "author": "",
  "license": "ISC"
}
